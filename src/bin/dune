(executables
 (names main compiler dfgCompiler functionCompiler dockerUtils eventParsers functionInterpreter)
 (libraries dpt)
 (link_flags -cc g++)
 )

(install
 (section bin)
 (files 
    (main.exe as dpt) 
    (functionInterpreter.exe as functionInterpreter)
    (compiler.exe as compiler) 
    (dfgCompiler.exe as dfgCompiler) 
    (functionCompiler.exe as functionCompiler)
    (dockerUtils.exe as dockerUtils)
    (eventParsers.exe as eventParsers)
    )
    )