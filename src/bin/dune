(executables
 (names main compiler functionCompiler dockerUtils)
 (libraries dpt)
 (link_flags -cc g++)
 )

(install
 (section bin)
 (files 
    (main.exe as dpt) 
    (compiler.exe as compiler) 
    (functionCompiler.exe as functionCompiler)
    (dockerUtils.exe as dockerUtils)
    )
    )