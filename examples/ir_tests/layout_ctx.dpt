global Array.t<<32>> foo = Array.create(1024);



entry event pktin(int src_ip, int dst_ip);

handle pktin(int src_ip, int dst_ip) {
    int x = 1;
    int y = x;
    int z = Array.get(foo, x);    
    if (z == 1){
        int p = 1;
    }
}